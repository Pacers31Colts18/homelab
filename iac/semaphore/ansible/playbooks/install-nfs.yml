---
- name: Install NFS client packages
  hosts: "{{ target_hosts | default('all') }}"
  become: true

  tasks:
    - name: Install nfs-common
      ansible.builtin.apt:
        name: nfs-common
        state: present
        update_cache: true
